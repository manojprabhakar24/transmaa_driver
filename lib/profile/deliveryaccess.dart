import 'package:flutter/material.dart';

class DeliveryAccessPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Delivery Access'),
      ),
      body: Center(
        child: Text('Delivery Access Page'),
      ),
    );
  }
}
